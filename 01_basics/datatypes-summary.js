// summary for datatypes

// Primitive

// there are  7 types in Primitive :  String, Number,Boolean, null,Undefined,Symbol,BigInt

/////////const score = 100

// JavaScript is a dynamically typed language.

// What that means:
// Variable types are determined at runtime, not in advance.

// You don’t need to declare a variable’s type explicitly — JavaScript figures it out based on the assigned value.//////////




//Reference (Non - Primitive)

// Arrays,Objects ( To master js just master objects & browser events in js ), Functions



 // objects in curly braces
 const heros = ["shaktiman", "naagraj", "doga"];

 let myObj = {
     name: "satyam",
     age: "20"
 };
 
 const myFunction = function() {
     console.log("Hello World");
 };
 
 console.log(typeof bigNumber);
 